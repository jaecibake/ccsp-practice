const QUESTIONS_CCZT_C3 = [
  { id:60301, domain:3, level:1, question:"What is the defining characteristic of a Software-Defined Perimeter (SDP)?", options:["It publishes all services openly so any client can discover and connect to them","It hides resources from unauthorized users, granting access only after authentication","It encrypts data at rest using keys managed exclusively by the cloud provider","It replaces identity providers by trusting any device with a valid IP address"], correct:1, explanation:"SDP makes protected resources invisible ('dark') to unauthorized parties and establishes connections only after the user and device are authenticated and authorized." },
  { id:60302, domain:3, level:2, question:"In the SDP model, which component carries the actual application data once a connection is authorized?", options:["The accepting host (gateway), which the controller instructs to allow the connection","The SDP controller, which also makes all the authentication decisions","The certificate authority, which signs each packet as it traverses the link","The SIEM, which inspects and forwards the traffic to its destination"], correct:0, explanation:"The accepting host (gateway) handles the authorized data connection; the controller only authenticates, authorizes, and tells gateways which connections to accept — it does not carry data." },
  { id:60303, domain:3, level:2, question:"What does Single Packet Authorization (SPA) accomplish before a TCP connection is allowed?", options:["It compresses the first packet to reduce the bandwidth used by the handshake","It proves the client's legitimacy so the service stays hidden from unauthorized scanners","It load-balances the connection across multiple available accepting hosts","It negotiates the encryption cipher suite used for the rest of the session"], correct:1, explanation:"SPA requires a cryptographically valid packet up front; until it is received, the service does not respond at all, keeping it invisible to port scans and unauthorized clients." },
  { id:60304, domain:3, level:1, question:"Why is an SDP often described as creating a 'black cloud'?", options:["Because it routes all traffic through anonymizing relays to hide user identity","Because protected resources are undetectable to unauthorized users and scanners","Because it stores data in an offline, air-gapped archive disconnected from networks","Because it darkens the user interface to reduce eye strain during long sessions"], correct:1, explanation:"The 'black cloud' concept refers to SDP making infrastructure invisible — unauthorized users cannot even detect that the resources exist, eliminating reconnaissance opportunities." },
  { id:60305, domain:3, level:2, question:"How does SDP improve on the traditional VPN model for remote access?", options:["It removes the need for any authentication by trusting the remote endpoint device","It eliminates encryption since the perimeter is already software-defined","It exposes the entire internal subnet so users can reach any system they need","It grants narrow per-resource access instead of broad network-level connectivity"], correct:3, explanation:"A VPN typically drops the user onto a network segment with broad reach; SDP instead grants least-privilege, per-resource connections, sharply reducing lateral movement risk." },
  { id:60306, domain:3, level:3, question:"An attacker performs a port scan against an organization's SDP-protected services and finds no open ports. Why?", options:["SDP uses default-drop with SPA, so services do not respond without prior authorization","The services are offline for scheduled maintenance during the scanning window","The attacker's scanning tool is incompatible with modern TCP/IP stacks","The organization has disabled all of its services to prevent the scan from working"], correct:0, explanation:"SDP enforces a default-drop posture; without a valid Single Packet Authorization, services never respond, so scanners see nothing — the resource is effectively invisible." },
  { id:60307, domain:3, level:2, question:"In SDP, what must occur before a client is allowed to connect to a protected service?", options:["The client must simply be located on the same physical network segment","The client must request the service through a public DNS lookup first","The client and its device must be authenticated and authorized by the controller","The client must wait for an administrator to manually approve the request"], correct:2, explanation:"The SDP controller authenticates the user and device and evaluates policy; only then does it instruct the gateway to accept that specific client's connection." },
  { id:60308, domain:3, level:1, question:"Which three roles make up the typical SDP architecture?", options:["Identity provider, antivirus agent, and backup server","Firewall, router, and switch operating in a coordinated mesh","Controller, initiating host (client), and accepting host (gateway)","SIEM, SOAR, and threat intelligence aggregation platform"], correct:2, explanation:"SDP consists of the controller (decisions), the initiating host (the client seeking access), and the accepting host or gateway (which enforces and carries the authorized connection)." },
  { id:60309, domain:3, level:2, question:"How does SDP align with the Zero Trust principle of 'never trust, always verify'?", options:["It verifies the device once at enrollment and trusts it thereafter indefinitely","It verifies users only when they attempt to access the most sensitive systems","It trusts all traffic that originates from previously registered IP addresses","It authenticates and authorizes every connection attempt before granting any access"], correct:3, explanation:"SDP embodies 'never trust, always verify' by requiring authentication and authorization for each connection before any access path is opened to a resource." },
  { id:60310, domain:3, level:3, question:"A company is concerned about credential theft enabling lateral movement. How does SDP specifically mitigate this risk?", options:["By granting only per-resource connections, so a stolen credential reaches far less","By storing all credentials in a single hardened vault appliance on-premises","By requiring users to change their passwords every twenty-four hours","By blocking all outbound internet traffic from internal user workstations"], correct:0, explanation:"Because SDP grants narrow, individualized access to specific resources rather than network-wide reach, a stolen credential exposes only what that subject was authorized for, limiting lateral movement." },
  { id:60311, domain:3, level:2, question:"What is the security advantage of SDP performing device validation in addition to user authentication?", options:["It allows the user to skip multi-factor authentication on trusted devices","It removes the need to encrypt the connection between client and gateway","It speeds up the connection by caching the device's prior authorization token","It ensures both the identity and the endpoint posture meet policy before access"], correct:3, explanation:"Validating the device alongside the user means a correct credential on a non-compliant or unknown device can still be denied, aligning with Zero Trust's device-aware access." },
  { id:60312, domain:3, level:1, question:"In SDP deployments, what does a 'default-drop' firewall posture mean?", options:["All inbound connections are accepted unless explicitly added to a block list","All connections are denied by default unless explicitly authorized through the controller","Connections are dropped only when the firewall is under heavy resource load","The firewall drops the slowest connections first to preserve overall performance"], correct:1, explanation:"Default-drop denies everything unless a connection has been explicitly authorized; combined with SPA, this is what keeps SDP-protected services invisible to unauthorized clients." },
  { id:60313, domain:3, level:2, question:"How can SDP be deployed to protect cloud-hosted workloads specifically?", options:["By placing accepting-host gateways in front of cloud workloads to broker authorized access","By disabling the cloud provider's native security groups entirely first","By moving all cloud workloads back on-premises before applying any controls","By exposing the workloads publicly and relying on logging to catch misuse"], correct:0, explanation:"SDP gateways can front cloud workloads, ensuring connections are brokered only after authentication and authorization — protecting cloud resources the same way as on-premises ones." },
  { id:60314, domain:3, level:3, question:"An organization deploys SDP but allows the controller to be reachable and unauthenticated from the internet. Why is this a serious design flaw?", options:["The controller will run slowly because of the additional internet traffic load","Controllers are never meant to be connected to any network whatsoever","The controller is the trust anchor; exposing it unauthenticated undermines the entire model","It violates a licensing restriction imposed by most SDP product vendors"], correct:2, explanation:"The controller makes all trust decisions; if it is reachable and unauthenticated, attackers can target the very component that authorizes access, collapsing SDP's security guarantees." },
  { id:60315, domain:3, level:2, question:"Which Zero Trust benefit is MOST directly delivered by SDP's resource-cloaking property?", options:["Faster data transfer speeds between authorized internal workloads","Lower licensing costs compared with traditional VPN concentrators","A dramatically reduced external attack surface for protected services","Automatic compliance with all applicable data privacy regulations"], correct:2, explanation:"By making resources invisible until authorization, SDP removes the externally reachable attack surface that scanners and unauthenticated attackers could otherwise probe." }
];
if (typeof window !== 'undefined') window.QUESTIONS_CCZT_C3 = QUESTIONS_CCZT_C3;
